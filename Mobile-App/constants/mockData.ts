// Re-export all types from the canonical types file.
// This file is kept for backward compatibility with any remaining imports.
export type { Vehicle, VehicleType, VehicleStatus, GeofenceConfig, FeedSummary, FeedStats, VehicleStats, PersistedVehicle } from './types';
